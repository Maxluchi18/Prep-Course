prototype: Cada vez que creamos un nuevo objeto de clase con un método, cada método que creamos se guarda en la memoria, es decir que se establece un método una vez para dar acceso a cada objeto de esa clase a esos métodos. 

Constructors (de Clases): Es una plantilla para crear objetos, nos permiten crear muchos objetos que comparten algunas de las mismas propiedades y métodos.